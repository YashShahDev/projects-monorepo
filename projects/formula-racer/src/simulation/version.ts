/**
 * Bump whenever vehicle behaviour changes. Best laps are stored per physics version, so a
 * lap set under different physics is never compared with a new one.
 */
export const PHYSICS_VERSION = "p3.9";
