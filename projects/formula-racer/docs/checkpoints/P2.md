---
id: CHECKPOINT-P2
title: Phase 2 checkpoint record
type: checkpoint
status: in_progress
date: 2026-09-26
updated: 2026-09-26
summary: Fixed-step vehicle simulation, controls/camera and tuning evidence.
---

# Phase 2 checkpoint record

Phase: [P2](../phases/P2-driving.md). The user authorized P2–P5 on 2026-09-26 with a
Codex review (`gpt-6-astra`) after each phase. Handling preferences and later-phase
product decisions were answered up front and recorded in each phase document.

## P2-C1 — Fixed-step vehicle simulation

State: done. Requires P1 (`ae25a13`).

Work: `FixedStepper` (60 Hz, bounded catch-up with reported dropped time,
interpolation alpha). Validated `CarDefinition` (`public/assets/cars/fr26.json`,
3.4 m wheelbase, 800 kg with driver). `createVehicleSimulation` builds a Rapier rigid
chassis with explicit mass properties and a four-wheel raycast vehicle controller;
consumers see `DriverControls` in and plain `VehicleSnapshot`s out. Reset,
disposal and `PHYSICS_VERSION` (`p2.1`). Shared content validators in
`src/content/validate.ts`.

Rapier facts found: `brake` is a per-step rolling-friction impulse cap (converted
from newtons × dt) and is ignored while a wheel has engine force; `frictionSlip`
behaves as the tyre friction coefficient; the forward-axis setter is the property
`setIndexForwardAxis`.

Evidence: 67 unit tests pass: stepping at 30/60/75/144/240 Hz gives 60 steps/s,
bounded catch-up, rest on four wheels (< 5 cm drift in 5 s), 0–100 km/h in
1.6–3.2 s (measured 2.05 s), bounded braking from 200 km/h, steering sign, control
clamping, reset, identical repeated trajectories and **bit-identical state at 30,
60 and 144 Hz render rates**. No drag yet, so top speed is unbounded until P2-C3.

## P2-C2 — Controls, assists, gears, camera and greybox track

State: done. Built test-first (red → green vertical slices), one seam at a time.

Test seams: `parseTrack`/`fetchTrack` (content boundary); `buildTrackGeometry` →
`pointAt`/`locate` (lap distance, signed lateral offset, surface); the shipped circuit's
layout requirements from [P4](../phases/P4-content.md); keyboard adapter (DOM events in,
`DriverControls` out); the driver-aid pipeline (raw input + snapshot in, assisted
controls out); the automatic gearbox (speed/throttle in, gear/rpm out); the chase camera
rig (snapshot in, camera pose out); the game session through the dev test hooks; and
browser checks (keys move the car, focus loss clears input and pauses).

Track: Harbour Park, 3.93 km, 12 m wide with 1.5 m kerbs, clockwise, with a 1.29 km
back straight, R18 hairpin, 90° R250 sweeper, 35° chicane and medium corners.
Tests assert those features, ≥ 25 m clearance between non-adjacent parts, and that the
start is on the road. Mutating the layout (soft chicane, wide hairpin) or the geometry
(lateral sign, stale-hint fallback) makes them fail.

Keyboard: arrows and WASD drive; R/C/Escape fire once per press (auto-repeat ignored);
game keys suppress page scrolling; window blur and a hidden tab release every key.

Assists (vehicle adapter, per wheel, from the previous step's load and cornering force).
Rapier halves forward impulse in its friction-circle test, so the car can brake at
about 3 g on μ 1.8 and its wheels cannot lock or spin. Assists therefore limit demand
against the grip budget rather than modulating wheel slip:

- ABS caps each wheel's brake force at 90% of its remaining longitudinal budget. Braking
  hard at full lock from 150 km/h: peak sideslip > 8° without, < 4° with; straight-line
  stopping from 200 km/h stays within 15% (a 50% margin fails that test).
- Traction control applies the same cap to rear drive force. On μ 0.5 (grass-like),
  flooring it out of a 20 km/h corner: peak rear-axle slip 5.4° without, 1.2° with. On
  the shipped μ 1.8 the rear barely slides, so TC seldom intervenes until P2-C3 surfaces.
- Steering assist caps the front angle at L·μ·g/v² + 0.015 rad (peak slip from fixed-lock
  sweeps: best lock ≈ 4° at 100 km/h, 2° at 180, ≤ 1° at 250). Full lock at 250 km/h
  turns further and keeps more speed; low-speed lock is untouched.

Keyboard smoothing (`createInputSmoother`): steering ramps to full lock in 0.25 s and
re-centres twice as fast; held-key lock is 1/√(1 + (v/30 m/s)²): 73% at 100 km/h, 40%
at 250 km/h. Pedals stay on/off; traction control and ABS handle the rest.

Gearbox (`createGearbox`, data in `fr26.json` `powertrain.gearbox`): eight gears topping
out at 95–345 km/h at a 12 500 rpm redline; upshift 11 800, downshift 8 000 rpm. Content
validation rejects gears that would land below the downshift point after an upshift, so
the box cannot hunt. One shift per update. Drive force is still power-limited, so gears
feed the HUD (and later audio) but do not yet change acceleration; P2-C3's powertrain
module can consume them.

Camera rig (`src/rendering/camera-rig.ts`, no Three.js): the chase view tracks the car's
position exactly, 6 m behind and 2.2 m up, and follows its heading with a 0.3 s
exponential lag, so turns read as the car rotating in frame; the lag decays per elapsed
second, so 30 and 144 fps give the same path. C cycles to a rigid nose camera; reset
drops the lag.

Session (`src/app/session.ts`, no DOM): starts the car on the track's start line facing
along the centreline; smooths input and updates the gearbox per simulation step (so the
render rate cannot change the result: 30 and 144 Hz end at the same lap distance);
Escape toggles pause, focus loss pauses, and the first frame after resuming is not
simulated, since it reports the whole paused gap; R resets car, smoothing, gears and
camera; C cycles the camera.

Greybox view (`src/rendering/track-view.ts`): road, kerb and start-line ribbons from the
track geometry, a box car with steered and spinning wheels, speed/gear readout and a
pause banner. It replaces the P1 probe app; the probe scene, world and view are removed
and `initPhysics` moved to `src/simulation/physics.ts`. The probe-scene tests' coverage of
the shared validators and `fetchJson` moved to the track tests.

Found while building it: the ribbons were back-face culled (outer offsets lie left of
inner ones, so the first winding faced down), visible as a road-less screenshot; the
pause banner waited for the next frame, so the HUD now updates on each action; and with
no drag a released throttle coasts at constant speed (+0.8 km/h as the chassis leaves
squat), so the focus-loss test asserts no acceleration rather than slowing.

Evidence: 107 unit tests; `make test-full` 31/31, including production builds with no
test hooks, subpath hosting and Firefox smoke. Production script 4.89 MB raw / 1.80 MB
gzip (unchanged from P1: Rapier's inlined WASM dominates).

## P2-C3 — Surfaces, aero, powertrain and tuning

State: in progress.

Aero (`fr26.json` `aero`, physics `p2.2`): drag ½ρ·CdA·v² with CdA 1.36 m² opposes the
velocity; downforce ½ρ·ClA·v² with ClA 4.0 m² presses along the chassis's down axis at
the axles. Top speed settles at 320–360 km/h (700 kW); coasting from 200 km/h matches
the drag-only prediction (170.4 km/h after 3 s). The steering assist's grip limit now
includes downforce.

Found and fixed, each with a failing test first:

- The chassis bottomed out and scraped the ground above ~290 km/h (suspension fully
  compressed, collider at y = 0), which capped top speed at 293 km/h. Rapier's spring
  force is about stiffness × compression × mass, so stiffness 70 left too little travel
  for 5.5 kN/wheel of downforce. Now 200 (compression 10, relaxation 8.5, scaled by
  √(200/70)); the chassis stays > 3 cm clear at top speed.
- 45% front downforce spun the car in fast corners (118° turned against a 53° kinematic
  path at 250 km/h with 0.03 rad). 38% tracks the path with mild understeer; tests
  bound the turn at 1.25× kinematic at 180 and 250 km/h.
- With downforce the rear no longer steps out when trail braking from 150 km/h, so the
  ABS test moved to 120 km/h with half lock (sideslip 30.9° without ABS, 1.9° with).

Surface grip (physics `p2.3`): tracks carry `surfaceGrip` multipliers (Harbour Park: road
1, kerb 0.85, grass 0.45); the session looks up each wheel's surface every step and the
vehicle applies it to that wheel's friction and to the ABS/TC budgets and the steering
assist. Braking from 150 km/h on 0.45 grip takes over 1.6× the road distance.

Known limit: above ~250 km/h downforce gives even the grass more grip than the brakes
can use (≈40 kN available against 32 kN of brakes at 300 km/h, helped by Rapier's 2×
longitudinal budget), so running wide at top speed is barely punished. A replaceable
tyre model (P2 follow-up) or grass drag could fix this; recorded rather than tuned now.

Powertrain (`src/simulation/powertrain.ts`, physics `p2.4`): engine power follows a
content power curve (45% at 4000 rpm, peak 10 500 rpm, 94% at the redline) over road
speed, capped at the 11 kN traction limit, which governs below ~230 km/h. Each upshift
cuts drive for 50 ms. The gearbox moved inside the vehicle adapter, and snapshots now
carry gear and rpm. Because the box shifts at 11 800 rpm, past the peak, an upshift lands
near 10 450 rpm, and drive rises slightly after the cut.
