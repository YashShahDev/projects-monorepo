---
id: CHECKPOINT-P0
title: Phase 0 checkpoint record
type: checkpoint
status: done
date: 2026-09-26
updated: 2026-09-26
summary: Documentation, toolchain installation and prerequisite verification evidence.
---

# Phase 0 checkpoint record

## P0-C1 — Documentation scaffold

State: done. Depends on approved plan and repository conventions.
Acceptance: root scaffolder used; linked plan/phase/decision/research documents exist;
agent workflow records main-checkout preference and independent-worktree guidance.

Branch: `feat/formula-racer`; base `ef7ad88`. Main checkout, per user steering.
Existing untracked `projects/text_tools.py` is outside scope.

Evidence: scaffold created with `make new PROJECT=formula-racer LANG=node`;
no nested Git repository. Product choices: solo time trial, accessible handling,
original car/teams/circuit, Bun toolchain. No game implementation or benchmark.

Verification: root `make test`, `make lint`, and `make build` with
`PROJECT=formula-racer` passed the explicitly labeled documentation-only targets.
`git diff --check` passed. No behavior tests apply to the documentation-only commit.

## P0-C2 — Toolchain

State: done. Requires P0-C1 (`35d4059`).

Pinned Bun/Node/GitHub CLI, Three.js/Rapier, TypeScript, Playwright and asset tooling.
Per user steering, Oxlint/Oxfmt replace ESLint and `bun test` is the unit test runner.
Committed lockfile, strict tool TypeScript configuration, Make targets, doctor,
prerequisite integration checks, local Markdown-link checks and 15 behavior tests.

Evidence: `make test PROJECT=formula-racer` passes 15 tests. `make lint
PROJECT=formula-racer` passes TypeScript, Oxlint, Oxfmt and documentation-link checks.
`make doctor` passes all tool-version and browser-executable checks. Browser/Blender/KTX
integration checks have passed; final reproduction evidence belongs to P0-C3.

One local finding: the user's GitHub CLI PATH wrapper invokes global mise setup even
for `--version`. Doctor resolves installed mise binaries directly; regression tests
cover bypassing the wrapper and failing without fallback when resolution fails.

Draft PR: [#7](https://github.com/YashShahDev/projects-monorepo/pull/7).

## P0-C3 — Verification and handoff

State: done. Requires P0-C2 (`1df7e26`). Verified on 2026-09-26, Linux x86_64/Omarchy,
Core Ultra 7 268V / Intel integrated Arc graphics, approximately 30 GiB RAM.

| Check                              | Actual result                                                                                                                                           |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `make setup`                       | Frozen lockfile install, 207 installs/262 packages checked without changes; pinned browsers present                                                     |
| `make doctor`                      | All version/presence checks passed: Bun 1.4.2, Node 24.21.0, gh 2.101.0, Blender 4.5.14, KTX 4.4.2, LFS 3.8.0                                           |
| `make verify-prerequisites`        | Chromium 153.0.8010.12 and Firefox 155.0 launched headlessly and executed DOM checks                                                                    |
| Blender background fixture         | Python succeeded; GLB 2.0 export 1,728 bytes; PNG exported                                                                                              |
| KTX fixture                        | UASTC KTX2 encoded, 272 bytes, expected file identifier                                                                                                 |
| glTF Transform 4.5.0               | Successfully inspected the Blender GLB                                                                                                                  |
| `make test PROJECT=formula-racer`  | 15 tests pass, 0 failures, 19 assertions                                                                                                                |
| `make lint PROJECT=formula-racer`  | TypeScript 7.0.2, Oxlint 1.85.0, Oxfmt 0.70.0 and local doc-file links pass                                                                             |
| `make build PROJECT=formula-racer` | Correctly reports no browser entrypoint before P1                                                                                                       |
| `git diff --check`                 | Pass                                                                                                                                                    |
| Git LFS                            | Local install and attributes verified; authenticated batch-download endpoint returned HTTP 200 and expected 404 for nonexistent probe object; no upload |

No system packages or window-manager commands were needed. Native archives were
SHA256-verified and installed in user-local directories. Setup includes reproducible
URLs/checksums and does not overwrite existing command symlinks.

A final frozen-install/doctor/integration pass succeeded after the configuration
changes. Integration checks create and clean disposable fixtures; no gameplay assets
were generated. No GPU/FPS or interactive Blender GUI claim is made.

## Handoff

Draft PR: [#7](https://github.com/YashShahDev/projects-monorepo/pull/7), stays draft.
All project work is checkpoint-committed; unrelated `projects/text_tools.py` remains
untracked. Durable architecture and the gh-wrapper finding are mirrored to
`~/Documents/tech/decisions/2026-09-26-formula-racer-bun-wasm-foundation.md`; the vault
index was regenerated (27 notes). That private mirror is not needed by other agents.

Next: [P1 brief](../phases/P1-technical.md), then P1-C1 HTML entrypoint/Bun build.
No pending product clarification. P1 must prove Bun/Three.js/Rapier integration;
P4 must prove actual LFS object transfer before large source assets. Checkpoint commits
are discoverable by ID, avoiding self-referential commit hashes in documents.
